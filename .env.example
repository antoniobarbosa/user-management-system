# Monorepo root — copy to `.env` and adjust for your environment

NODE_ENV=development

POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=backoffice
POSTGRES_PORT=5432

# Connection URL for the backend (Prisma). In Docker Compose, use hostname `postgres`.
DATABASE_URL=postgresql://app:app@localhost:5432/backoffice
