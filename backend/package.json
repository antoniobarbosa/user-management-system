{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && tsc-alias",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "db:setup:test": "dotenv -e .env.test -- node --input-type=commonjs -e \"const { execSync } = require('child_process'); const d = process.env.DATABASE_URL; if (!d) throw new Error('DATABASE_URL missing in .env.test'); const u = new URL(d.replace(/^postgresql:/i, 'http:')); const db = u.pathname.slice(1).split('?')[0]; const h = u.hostname; const p = u.port || '5432'; const user = decodeURIComponent(u.username || ''); const pass = decodeURIComponent(u.password || ''); try { execSync('createdb -h ' + h + ' -p ' + p + ' -U ' + user + ' ' + db, { stdio: 'inherit', env: { ...process.env, PGPASSWORD: pass } }); } catch (e) { console.log('DB already exists, skipping'); }\" && npm run db:migrate:test",
    "dev:test": "npm run db:setup:test && dotenv -e .env.test -- tsx watch src/index.ts",
    "db:migrate:test": "dotenv -e .env.test -- prisma migrate deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@prisma/client": "^6.6.0",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.4.0",
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^22.14.0",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^4.1.4",
    "dotenv-cli": "^8.0.0",
    "pino-pretty": "^13.1.3",
    "prisma": "^6.6.0",
    "supertest": "^7.1.0",
    "tsc-alias": "^1.8.16",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": ">=22"
  }
}
