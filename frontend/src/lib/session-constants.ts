/** Cookie lido pelo middleware (Edge não acede a `localStorage`). */
export const SESSION_COOKIE_NAME = "um_session_id";

/** Chave `localStorage` do `persist` do Zustand em `sessionStore.ts`. */
export const UM_SESSION_STORAGE_KEY = "um_session";
