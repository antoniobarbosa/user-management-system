/** Cookie read by middleware (Edge cannot access `localStorage`). */
export const SESSION_COOKIE_NAME = "um_session_id";

/** `localStorage` key for Zustand `persist` in `sessionStore.ts`. */
export const UM_SESSION_STORAGE_KEY = "um_session";
